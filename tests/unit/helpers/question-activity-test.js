import { questionActivity } from 'qa-board/helpers/question-activity';
import { module, test } from 'qunit';

module('Unit | Helper | question activity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionActivity([42]);
  assert.ok(result);
});
